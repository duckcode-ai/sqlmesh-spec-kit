# Writing SQLMesh Model Specs

Use this when drafting specs for SQLMesh model changes.

Checklist:

- Name affected models, upstream dependencies, downstream consumers, and one-row grain.
- Keep acceptance criteria in EARS format.
- State required audits, tests, and `sqlmesh plan <env>` evidence.
- Do not prescribe implementation until the spec is approved.
