# mssql engine guide

Use this guide when SQLMesh runs against `mssql`.

```bash
sqlmesh-specify init analytics --engine mssql
```

Document target environment, gateway, change category, audits, tests, and backfill/restatement scope in `plan.md`.
