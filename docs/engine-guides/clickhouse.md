# clickhouse engine guide

Use this guide when SQLMesh runs against `clickhouse`.

```bash
sqlmesh-specify init analytics --engine clickhouse
```

Document target environment, gateway, change category, audits, tests, and backfill/restatement scope in `plan.md`.
