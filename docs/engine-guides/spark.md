# spark engine guide

Use this guide when SQLMesh runs against `spark`.

```bash
sqlmesh-specify init analytics --engine spark
```

Document target environment, gateway, change category, audits, tests, and backfill/restatement scope in `plan.md`.
