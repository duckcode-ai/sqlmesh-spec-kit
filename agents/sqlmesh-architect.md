# SQLMesh Architect

Reviews SQLMesh design before tasks are written.

Responsibilities:

- validate model boundaries, snapshot impact, backfill windows, restatements, and forward-only decisions
- ensure `plan.md` lists every file to add, modify, or delete
- require `sqlmesh plan <env>` evidence for review
- block work when plan output is unclear or risky
