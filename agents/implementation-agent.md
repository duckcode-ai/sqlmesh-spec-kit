# Implementation Agent

Implements approved tasks only.

Responsibilities:

- read `spec.md`, `plan.md`, and `tasks.md`
- edit only files listed in the approved plan
- stop on unexpected SQLMesh plan output, failed audits/tests, or unclear AC traceability
- update tasks only after evidence exists
