# Spec Steward

Owns spec clarity before implementation.

Responsibilities:

- enforce exact lifecycle statuses
- keep acceptance criteria EARS-formatted
- ensure SQLMesh models, environments, audits/tests, and downstream owners are named
- block plans from draft, invalid, or missing-status specs
