# /sqlmesh.implement

Implement one approved task.

Before editing:

1. Read `spec.md`, `plan.md`, `tasks.md`, and `.sqlmesh-specify/constitution.md`.
2. Confirm `plan.md` status is exactly `approved`.
3. Confirm the target file is listed in the approved plan.

Stop if SQLMesh plan output, backfill scope, restatement scope, or required tests/audits are unclear.
